// Lecții C++ — extensia OOP a lui C

const cppLessons = [
  {
    slug: "cpp-introducere",
    title: "1. Introducere C++ + Hello World",
    order: 1,
    theory: [
      { order: 1, title: "Ce e C++?", content: "**C++** (1979, Bjarne Stroustrup) extinde C cu:\n• **OOP** (clase, moștenire, polimorfism)\n• **Templates** (generic programming)\n• **STL** (Standard Template Library — vectors, maps, algorithms)\n• **RAII** (resource management automat)\n• **Exception handling**\n\n**Folosit în:** game engines (Unreal, Unity backend), browsers (Chrome, Firefox), Adobe products, sisteme de tradeing financiar, embedded.\n\n```cpp\n#include <iostream>\n\nint main() {\n  std::cout << \"Salut, lume!\" << std::endl;\n  return 0;\n}\n```" },
      { order: 2, title: "Diferențe cheie față de C", content: "```cpp\n// C++\n#include <iostream>      // în loc de stdio.h\nstd::cout << \"text\";    // în loc de printf\nstd::cin >> x;            // în loc de scanf\n\n// using namespace pentru cod mai scurt\nusing namespace std;     // (debate — ok pentru școală, evită în production)\ncout << \"text\" << endl;\n\n// C++ acceptă C aproape integral\nprintf(\"merge și asta\\n\");  // legacy\n\n// Bool nativ\nbool activ = true;        // în C ai nevoie de stdbool.h\n\n// Comentarii cu //\n// Strings reale cu std::string\n```\n\n**Compilare cu g++:**\n```bash\ng++ hello.cpp -o hello -std=c++17\n./hello\n```" },
      { order: 3, title: "Operatori cout/cin", content: "```cpp\n#include <iostream>\nusing namespace std;\n\nint main() {\n  // Output cu <<\n  int a = 5;\n  double b = 3.14;\n  string nume = \"Ana\";\n  cout << \"a = \" << a << \", b = \" << b << \", nume = \" << nume << endl;\n\n  // Input cu >>\n  int varsta;\n  cout << \"Vârsta: \";\n  cin >> varsta;\n\n  // Citire string cu spații\n  string nume2;\n  getline(cin, nume2);\n\n  return 0;\n}\n```\n\nOperatorii `<<` și `>>` sunt **overloaded** — funcționează cu orice tip standard." },
    ],
    tasks: [
      { number: 1, name: "C++ extensie", question: "C++ extinde C cu:", options: ["Doar sintaxă", "OOP, templates, STL, RAII", "Database", "Networking"], answer: "OOP, templates, STL, RAII", explanation: "C++ păstrează compatibilitatea cu C și adaugă abstracțiuni de înalt nivel.", difficulty: "easy" },
      { number: 2, name: "cout", question: "Ce face std::cout << \"x\"?", options: ["Citire", "Output în consolă (similar cu printf)", "Compare", "Cast"], answer: "Output în consolă (similar cu printf)", explanation: "cout = console output. << e operator de inserție în stream.", difficulty: "easy" },
      { number: 3, name: "endl", question: "Ce face std::endl?", options: ["Print '\\n' + flush stream", "Doar newline", "EOF", "End program"], answer: "Print '\\n' + flush stream", explanation: "endl = newline + flush. Pentru doar newline mai performant: \"\\n\".", difficulty: "medium" },
      { number: 4, name: "Compile", question: "Cum compilezi cu standard C++17?", options: ["g++ file.cpp", "g++ file.cpp -std=c++17 -o exe", "compile file.cpp", "cpp file.cpp"], answer: "g++ file.cpp -std=c++17 -o exe", explanation: "-std=c++17 pentru features moderne. Default g++ e mai vechi.", difficulty: "medium" },
      { number: 5, name: "namespace std", question: "Pentru ce e using namespace std;?", options: ["Importă lib", "Permite cout în loc de std::cout (poluează namespace)", "Activează C++", "Niciuna"], answer: "Permite cout în loc de std::cout (poluează namespace)", explanation: "Convenabil dar riscant în production (conflicte de nume). Preferă std:: explicit în headere.", difficulty: "medium" },
      { number: 6, name: "string", question: "C++ are tip nativ pentru string?", options: ["Nu", "Da — std::string", "Doar char[]", "Doar prin biblioteci externe"], answer: "Da — std::string", explanation: "std::string (din <string>) e clasa standard. Spre deosebire de C unde folosești char[].", difficulty: "easy" },
      { number: 7, name: "cin spaces", question: "Pentru a citi linie cu spații:", options: ["cin >> str", "getline(cin, str)", "scanf", "fgets"], answer: "getline(cin, str)", explanation: "cin >> se oprește la whitespace. getline citește până la newline.", difficulty: "medium" },
      { number: 8, name: "C compatibility", question: "Cod C compilează în C++?", options: ["Da, mereu", "Aproape — există mici diferențe (cast-uri void*, keywords)", "Nu", "Doar C89"], answer: "Aproape — există mici diferențe (cast-uri void*, keywords)", explanation: "C++ nu e strict superset. Ex: void* trebuie cast explicit. Dar majoritatea codului C compilează.", difficulty: "hard" },
    ],
  },
  {
    slug: "cpp-vectori-string",
    title: "2. std::vector și std::string",
    order: 2,
    theory: [
      { order: 1, title: "std::vector — array dinamic", content: "Spre deosebire de array C cu mărime fixă, **vector** se redimensionează automat:\n\n```cpp\n#include <vector>\n#include <iostream>\nusing namespace std;\n\nint main() {\n  vector<int> v;             // gol\n  vector<int> v2(5);          // 5 zero-uri\n  vector<int> v3(5, 100);     // 5 elemente, toate 100\n  vector<int> v4 = {1, 2, 3, 4, 5};  // initializer list\n\n  v4.push_back(6);            // adaugă la final: {1,2,3,4,5,6}\n  v4.pop_back();              // elimină ultimul\n  v4[0] = 100;                 // modificare\n  cout << v4[0];               // 100\n  cout << v4.size();           // 5\n  cout << v4.empty();          // false\n  v4.clear();                  // golește\n\n  // Iterare\n  for (int n : v4) cout << n << \" \";\n  for (size_t i = 0; i < v4.size(); i++) cout << v4[i];\n}\n```" },
      { order: 2, title: "std::string — text proper", content: "```cpp\n#include <string>\nusing namespace std;\n\nstring s1 = \"Salut\";\nstring s2(\"lume\");\nstring s3 = s1 + \", \" + s2 + \"!\";  // concatenare cu +\ns1 += \"!\";                            // append\n\ncout << s3.length();          // 12\ncout << s3.size();             // identic\ncout << s3.substr(7, 4);       // \"lume\"\ncout << s3.find(\"lume\");       // 7 (poziție)\ncout << s3[0];                  // 'S'\n\nif (s1 == \"Salut!\") { /* ok */ }       // == funcționează direct!\nif (s1.empty()) { /* gol? */ }\ns1.clear();\n\n// Conversie\nint n = stoi(\"42\");                     // string to int\nstring s = to_string(3.14);              // num to string\n```" },
      { order: 3, title: "Iterare modernă cu range-based for", content: "```cpp\n#include <vector>\n#include <string>\nusing namespace std;\n\nvector<int> v = {1, 2, 3, 4, 5};\n\n// Range-based for (C++11+) — cel mai curat\nfor (int x : v) cout << x;\n\n// Cu modificare — folosește referință\nfor (int& x : v) x *= 2;  // dublează in-place\n\n// Const referință (eficient pentru tipuri mari)\nvector<string> nume = {\"Ana\", \"Ion\"};\nfor (const string& n : nume) cout << n << endl;\n\n// Auto pentru tip dedus\nfor (auto& x : v) cout << x;\n\n// Cu index — folosește for clasic\nfor (size_t i = 0; i < v.size(); i++) {\n  cout << i << \": \" << v[i] << endl;\n}\n```" },
    ],
    tasks: [
      { number: 1, name: "vector size", question: "Cum afli numărul de elemente într-un vector?", options: ["v.length()", "v.size()", "len(v)", "sizeof(v)"], answer: "v.size()", explanation: ".size() returnează numărul de elemente. .length() merge la string dar size() funcționează la ambele.", difficulty: "easy" },
      { number: 2, name: "push_back", question: "Cum adaugi un element la finalul vectorului?", options: ["v.add(x)", "v.push_back(x)", "v.append(x)", "v[end] = x"], answer: "v.push_back(x)", explanation: "push_back amortizează O(1). Vector se redimensionează automat dacă capacitatea e depășită.", difficulty: "easy" },
      { number: 3, name: "Range-based for", question: "Care e sintaxa modernă C++11 pentru iterare?", options: ["for x in v", "for (int x : v)", "v.forEach(...)", "for (auto x of v)"], answer: "for (int x : v)", explanation: "Range-based for. Cu auto& sau const auto& pentru eficiență/modificare.", difficulty: "easy" },
      { number: 4, name: "string +", question: "Pot face s1 + s2 pentru string?", options: ["Nu", "Da, std::string overrideează operator+", "Doar cu strcat", "Eroare"], answer: "Da, std::string overrideează operator+", explanation: "Spre deosebire de char* C, std::string suportă operator+ pentru concatenare directă.", difficulty: "easy" },
      { number: 5, name: "string ==", question: "Pot folosi == pentru a compara string-uri?", options: ["Nu", "Da — std::string overrideează operator==", "Doar cu strcmp", "Doar cu .equals()"], answer: "Da — std::string overrideează operator==", explanation: "string s1 == s2 compară conținutul. Spre deosebire de char* unde == compară pointers.", difficulty: "medium" },
      { number: 6, name: "vector init", question: "Cum inițializezi vector cu {1,2,3}?", options: ["vector<int> v[1,2,3]", "vector<int> v = {1,2,3}", "vector<int>(1,2,3)", "vector<int>::of(1,2,3)"], answer: "vector<int> v = {1,2,3}", explanation: "Initializer list (C++11+). Sau vector<int> v{1,2,3}; sau vector<int> v({1,2,3}).", difficulty: "easy" },
      { number: 7, name: "auto", question: "Ce face cuvântul cheie auto în C++11?", options: ["Variabilă globală", "Tipul e dedus automat de compilator", "Auto-init", "Pointer auto"], answer: "Tipul e dedus automat de compilator", explanation: "auto x = expression; — compilator deduce tipul. Util pentru tipuri lungi (iteratori).", difficulty: "medium" },
      { number: 8, name: "vector vs C array", question: "Avantaj vector vs C array?", options: ["Niciun", "Dimensiune dinamică, size() built-in, automatic memory management", "Mai rapid", "Mai puțin overhead"], answer: "Dimensiune dinamică, size() built-in, automatic memory management", explanation: "vector se redimensionează automat, are .size(), bounds-checking cu .at(). Performanță similară cu C array.", difficulty: "medium" },
    ],
  },
  {
    slug: "cpp-clase",
    title: "3. Clase și obiecte",
    order: 3,
    theory: [
      { order: 1, title: "Definirea unei clase", content: "```cpp\n#include <iostream>\n#include <string>\nusing namespace std;\n\nclass Persoana {\n private:\n  string nume;       // accesibil doar din clasa\n  int varsta;\n\n public:\n  // Constructor\n  Persoana(string n, int v) : nume(n), varsta(v) {}\n\n  // Metode\n  void afiseaza() {\n    cout << nume << \" are \" << varsta << \" ani\" << endl;\n  }\n\n  // Getter\n  string getNume() const { return nume; }\n\n  // Setter\n  void setVarsta(int v) {\n    if (v >= 0) varsta = v;\n  }\n};\n\nint main() {\n  Persoana p(\"Ana\", 25);\n  p.afiseaza();\n  cout << p.getNume();\n  p.setVarsta(26);\n  return 0;\n}\n```" },
      { order: 2, title: "Encapsulare: public, private, protected", content: "**3 nivele de acces:**\n\n```cpp\nclass MyClass {\n public:\n  int public_field;        // acces oriunde\n  void publicMethod() { }\n\n private:\n  int private_field;       // doar din interiorul clasei\n  void privateMethod() { }\n\n protected:\n  int protected_field;     // clasa + clasele derivate\n};\n```\n\n**Default-ul în `class`**: private. Default-ul în `struct`: public. (Singura diferență tehnică.)\n\n**Best practice OOP:**\n• Date = private\n• Interface = public (metode)\n• Acces controlat prin getters/setters\n\n**`const` pe metode** = nu modifică starea clasei:\n```cpp\nstring getNume() const { return nume; }\n// Compilatorul previne nume = \"X\" în această metodă\n```" },
      { order: 3, title: "Constructori multipli + destructor", content: "```cpp\nclass Persoana {\n  string nume;\n  int varsta;\n  int* date;  // alocare dinamică\n\n public:\n  // Default constructor\n  Persoana() : nume(\"Necunoscut\"), varsta(0), date(nullptr) {}\n\n  // Parametrized\n  Persoana(string n, int v) : nume(n), varsta(v) {\n    date = new int[100];  // alocare\n  }\n\n  // Copy constructor\n  Persoana(const Persoana& other) : nume(other.nume), varsta(other.varsta) {\n    date = new int[100];\n    // copy data\n  }\n\n  // Destructor — eliberează resurse\n  ~Persoana() {\n    delete[] date;\n  }\n};\n```\n\n**Rule of Three:** Dacă ai destructor custom, probabil ai nevoie și de copy constructor + assignment operator. Modern C++: **Rule of Five** (move constructor, move assignment)." },
    ],
    tasks: [
      { number: 1, name: "class default", question: "Default access în `class`?", options: ["public", "private", "protected", "internal"], answer: "private", explanation: "În class, membri sunt private by default. În struct sunt public. Asta e singura diferență tehnică.", difficulty: "medium" },
      { number: 2, name: "Constructor", question: "Ce face un constructor?", options: ["Nimic", "Inițializează obiectul când e creat", "Distruge obiectul", "Copiază"], answer: "Inițializează obiectul când e creat", explanation: "Constructor se apelează automat la new sau declarare. Nume = numele clasei. Fără tip return.", difficulty: "easy" },
      { number: 3, name: "Destructor", question: "Cum scrii un destructor?", options: ["~ClassName()", "destroy()", "delete()", "free()"], answer: "~ClassName()", explanation: "~ClassName() — apelat automat la ștergerea obiectului. Eliberează resurse (memory, files).", difficulty: "medium" },
      { number: 4, name: "Member init list", question: "Ce e Persoana(string n) : nume(n) {}?", options: ["Eroare", "Member initializer list — eficient + obligatoriu pentru const/reference", "Comentariu", "Try-catch"], answer: "Member initializer list — eficient + obligatoriu pentru const/reference", explanation: "Inițializează membrii înainte de body-ul constructorului. Mai eficient decât asignare în body.", difficulty: "hard" },
      { number: 5, name: "private", question: "Membrii private sunt accesibili:", options: ["Oriunde", "Doar din clasa proprie (și friends)", "Din clase derivate", "Public"], answer: "Doar din clasa proprie (și friends)", explanation: "Encapsulare strictă. Pentru access din clase derivate folosești protected.", difficulty: "medium" },
      { number: 6, name: "const method", question: "Ce face `const` la finalul declarației metodei?", options: ["Nimic", "Promisiune că metoda nu modifică starea obiectului", "Read-only", "Static"], answer: "Promisiune că metoda nu modifică starea obiectului", explanation: "Compilatorul previne modificări în metoda const. Apelabilă pe obiecte const.", difficulty: "hard" },
      { number: 7, name: "Object on stack", question: "Persoana p; — unde e alocat?", options: ["Heap", "Stack — eliberat automat la sfârșit scope", "Global", "Random"], answer: "Stack — eliberat automat la sfârșit scope", explanation: "Variabile locale sunt pe stack. Destructor apelat automat la ieșire din scope (RAII).", difficulty: "medium" },
      { number: 8, name: "new", question: "Persoana* p = new Persoana(); — ce face?", options: ["Stack alloc", "Heap alloc — necesită delete pentru cleanup", "Doar declarare", "Eroare"], answer: "Heap alloc — necesită delete pentru cleanup", explanation: "new alocă pe heap. Trebuie delete p; pentru cleanup. Modern C++: smart pointers (unique_ptr, shared_ptr).", difficulty: "medium" },
    ],
  },
  {
    slug: "cpp-mostenire-polimorfism",
    title: "4. Moștenire și polimorfism",
    order: 4,
    theory: [
      { order: 1, title: "Moștenire — clase derivate", content: "```cpp\nclass Animal {\n protected:\n  string nume;\n public:\n  Animal(string n) : nume(n) {}\n  void respira() { cout << nume << \" respiră\\n\"; }\n  virtual void vorbeste() { cout << \"sunet\\n\"; }\n  virtual ~Animal() {}  // virtual destructor!\n};\n\nclass Caine : public Animal {\n public:\n  Caine(string n) : Animal(n) {}  // apelează constructorul părinte\n  void vorbeste() override {  // C++11+: override e best practice\n    cout << nume << \" zice: Ham!\\n\";\n  }\n  void joaca() { cout << nume << \" se joacă\\n\"; }\n};\n\nint main() {\n  Caine rex(\"Rex\");\n  rex.respira();   // moștenită\n  rex.vorbeste();  // override-uită\n  rex.joaca();     // proprie\n  return 0;\n}\n```\n\n**Tipuri de moștenire:**\n• `: public Base` — IS-A relationship\n• `: protected Base` — moștenire restrânsă\n• `: private Base` — implementation reuse" },
      { order: 2, title: "Polimorfism via virtual", content: "**Virtual** permite overriding la runtime:\n\n```cpp\nAnimal* animale[] = {\n  new Caine(\"Rex\"),\n  new Pisica(\"Miau\"),\n  new Pasare(\"Cipi\")\n};\n\nfor (Animal* a : animale) {\n  a->vorbeste();  // apelează versiunea CORECTĂ pentru fiecare\n}\n// Output:\n// Rex zice: Ham!\n// Miau zice: Miau!\n// Cipi zice: Tweet!\n\n// Cleanup\nfor (Animal* a : animale) delete a;\n```\n\nFără `virtual`, ar apela mereu Animal::vorbeste() (static dispatch).\n**Rule:** dacă o clasă e proiectată pentru moștenire, destructorul TREBUIE să fie virtual." },
      { order: 3, title: "Clase abstracte (pure virtual)", content: "Clase care nu pot fi instanțiate, doar moștenite:\n\n```cpp\nclass Forma {\n public:\n  virtual double aria() = 0;        // pure virtual\n  virtual double perimetru() = 0;\n  virtual ~Forma() {}\n};\n\nclass Cerc : public Forma {\n  double raza;\n public:\n  Cerc(double r) : raza(r) {}\n  double aria() override { return 3.14 * raza * raza; }\n  double perimetru() override { return 2 * 3.14 * raza; }\n};\n\nint main() {\n  // Forma f;  // ❌ EROARE — clasă abstractă\n  Cerc c(5);  // ✅\n  cout << c.aria();\n  Forma* f = &c;  // ✅ pointer la clasă abstractă\n  cout << f->aria();  // 78.5 (polymorphism)\n}\n```\n\n**Pure virtual** = `= 0` — declarată dar fără implementare. Forțează clasele derivate să o implementeze." },
    ],
    tasks: [
      { number: 1, name: "Inheritance syntax", question: "Cum moștenești de la Animal?", options: ["class Caine extends Animal", "class Caine : public Animal", "class Caine inherits Animal", "class Caine(Animal)"], answer: "class Caine : public Animal", explanation: "Sintaxa C++: ': access Base'. public preserva accesul (cel mai uzual).", difficulty: "easy" },
      { number: 2, name: "virtual", question: "De ce ai nevoie de virtual?", options: ["Stilizare", "Pentru polimorfism — apelarea metodei din clasa derivată prin pointer la base", "Performance", "Sintaxă"], answer: "Pentru polimorfism — apelarea metodei din clasa derivată prin pointer la base", explanation: "Fără virtual, compilatorul cheamă versiunea de la tipul pointer-ului, nu cea reală a obiectului.", difficulty: "hard" },
      { number: 3, name: "override", question: "Ce face cuvântul cheie override?", options: ["Forțează virtual", "Verifică la compilare că metoda chiar override-uiește o virtual din base", "Crash", "Nimic"], answer: "Verifică la compilare că metoda chiar override-uiește o virtual din base", explanation: "override (C++11+) previne bug-uri de typo. Dacă semnătura nu match-uiește base, eroare compilare.", difficulty: "medium" },
      { number: 4, name: "Pure virtual", question: "Ce e o pure virtual function?", options: ["Eroare", "= 0; — declarată fără implementare; clasa devine abstractă", "Final", "Static"], answer: "= 0; — declarată fără implementare; clasa devine abstractă", explanation: "virtual void f() = 0; — clasele derivate TREBUIE să o implementeze. Clasa cu pure virtual = abstract.", difficulty: "medium" },
      { number: 5, name: "Abstract class", question: "Pot instanția o clasă abstractă?", options: ["Da", "Nu — doar prin clase derivate care implementează toate pure virtuals", "Doar pe heap", "Doar cu new"], answer: "Nu — doar prin clase derivate care implementează toate pure virtuals", explanation: "Abstract class = blueprint pentru moștenire. Forma f; → eroare. Doar Cerc/Pătrat/etc.", difficulty: "medium" },
      { number: 6, name: "Virtual destructor", question: "De ce destructor virtual e important?", options: ["Stilizare", "Pentru ca delete pe pointer base să apeleze și destructorul derived", "Performance", "Niciuna"], answer: "Pentru ca delete pe pointer base să apeleze și destructorul derived", explanation: "Fără virtual destructor, delete Animal* care pointează la Caine apelează doar ~Animal — leak resurse Caine.", difficulty: "hard" },
      { number: 7, name: "Multiple inheritance", question: "C++ suportă moștenire multiplă?", options: ["Nu", "Da — class C : public A, public B {}", "Doar interfaces", "Java only"], answer: "Da — class C : public A, public B {}", explanation: "C++ permite multiple inheritance. Atenție la 'diamond problem' — folosește virtual inheritance.", difficulty: "hard" },
      { number: 8, name: "protected", question: "Membri protected sunt accesibili:", options: ["Public", "Doar în clasa proprie", "Clasa proprie + clase derivate (NU exterior)", "Oriunde"], answer: "Clasa proprie + clase derivate (NU exterior)", explanation: "protected = privat pentru exterior, dar accesibil moștenitorilor. Util pentru API-uri internale.", difficulty: "medium" },
    ],
  },
  {
    slug: "cpp-templates",
    title: "5. Templates — generic programming",
    order: 5,
    theory: [
      { order: 1, title: "Funcții template", content: "**Template** = cod care funcționează pentru ORICE tip:\n\n```cpp\n// Fără template — duplicare\nint maxim_int(int a, int b) { return a > b ? a : b; }\ndouble maxim_double(double a, double b) { return a > b ? a : b; }\n\n// Cu template — un singur cod\ntemplate <typename T>\nT maxim(T a, T b) {\n  return a > b ? a : b;\n}\n\nint main() {\n  cout << maxim(3, 7);          // int — 7\n  cout << maxim(2.5, 1.8);      // double — 2.5\n  cout << maxim(\"abc\", \"abd\");   // string compare\n  return 0;\n}\n```\n\n**Compilatorul generează automat versiunile** pentru fiecare tip folosit. Type-safe + zero overhead la runtime." },
      { order: 2, title: "Clase template", content: "```cpp\ntemplate <typename T>\nclass Stack {\n  vector<T> elements;\n public:\n  void push(T x) { elements.push_back(x); }\n  T pop() {\n    T x = elements.back();\n    elements.pop_back();\n    return x;\n  }\n  bool empty() { return elements.empty(); }\n  size_t size() { return elements.size(); }\n};\n\nint main() {\n  Stack<int> ints;\n  ints.push(1);\n  ints.push(2);\n  cout << ints.pop();  // 2\n\n  Stack<string> strings;\n  strings.push(\"hello\");\n}\n```\n\nAcesta e modul cum sunt implementate `vector<T>`, `map<K,V>`, etc. în STL." },
      { order: 3, title: "Multiple type parameters", content: "```cpp\ntemplate <typename Key, typename Value>\nclass Pair {\n public:\n  Key first;\n  Value second;\n  Pair(Key k, Value v) : first(k), second(v) {}\n};\n\nPair<string, int> p(\"varsta\", 25);\ncout << p.first << \" = \" << p.second;\n\n// std::pair (built-in)\n#include <utility>\npair<string, int> p2 = {\"oras\", 42};\n\n// Template cu valori non-type (constante)\ntemplate <typename T, int N>\nclass FixedArray {\n  T data[N];\n public:\n  T& operator[](int i) { return data[i]; }\n};\n\nFixedArray<int, 10> arr;  // array de 10 int\n```" },
    ],
    tasks: [
      { number: 1, name: "Template", question: "Ce e un template în C++?", options: ["HTML", "Cod care funcționează pentru orice tip — compilatorul generează versiuni concrete", "Macro", "Definition"], answer: "Cod care funcționează pentru orice tip — compilatorul generează versiuni concrete", explanation: "Templates = generic programming. Compilatorul generează cod special pentru fiecare tip folosit.", difficulty: "medium" },
      { number: 2, name: "typename T", question: "Ce face template <typename T>?", options: ["Variabilă", "Declară T ca tip generic — folosit în funcția/clasa următoare", "Eroare", "Static"], answer: "Declară T ca tip generic — folosit în funcția/clasa următoare", explanation: "typename T (sau class T) = parametru tip. Înlocuit la utilizare cu tipul concret (int, string, etc).", difficulty: "easy" },
      { number: 3, name: "Auto deduce", question: "max(3, 7) cu max template — compilatorul deduce T=int automat?", options: ["Nu", "Da — template argument deduction", "Trebuie max<int>(3,7)", "Eroare"], answer: "Da — template argument deduction", explanation: "Compilatorul deduce tipul din argumente. Pentru ambiguități, specifici explicit: max<int>(...).", difficulty: "medium" },
      { number: 4, name: "STL vector", question: "vector<int> e:", options: ["Tip primitiv", "Class template instanțiat cu T=int", "Macro", "Function"], answer: "Class template instanțiat cu T=int", explanation: "vector e template — compilatorul creează versiunea pentru int la prima folosire vector<int>.", difficulty: "medium" },
      { number: 5, name: "Performance", question: "Templates au overhead la runtime?", options: ["Mare overhead", "Zero overhead — compilatorul generează cod specific", "Mediu", "GC"], answer: "Zero overhead — compilatorul generează cod specific", explanation: "Spre deosebire de generics Java (type erasure), C++ templates produc cod specializat — la fel de rapid ca cod scris manual.", difficulty: "hard" },
      { number: 6, name: "Multiple params", question: "template <typename K, typename V> — câte tipuri?", options: ["1", "2 — K și V", "3", "Variabil"], answer: "2 — K și V", explanation: "Multiple type parameters separate cu virgulă. Fiecare poate fi tip diferit.", difficulty: "easy" },
      { number: 7, name: "Compilation", question: "Templates compilează în .cpp sau în .h?", options: ["Doar .cpp", "În .h (sau în .tpp incluse) — compilatorul are nevoie de definiție când e folosit", "Run-time", "Niciuna"], answer: "În .h (sau în .tpp incluse) — compilatorul are nevoie de definiție când e folosit", explanation: "Spre deosebire de funcții normale (declare în .h, define în .cpp), templates trebuie definite în header.", difficulty: "hard" },
      { number: 8, name: "STL", question: "Ce e STL?", options: ["Standard Template Library — colecție de template-uri (vector, map, algorithms)", "STL e bază de date", "Compilator", "Network"], answer: "Standard Template Library — colecție de template-uri (vector, map, algorithms)", explanation: "STL = vector, map, set, algorithm (sort, find), iostream. Inima C++ modern.", difficulty: "easy" },
    ],
  },
  {
    slug: "cpp-stl-algorithms",
    title: "6. STL: containers și algorithms",
    order: 6,
    theory: [
      { order: 1, title: "Containers principale", content: "```cpp\n#include <vector>      // array dinamic\n#include <list>         // doubly linked list\n#include <map>          // sorted key-value (BST)\n#include <unordered_map> // hash map\n#include <set>          // sorted unique\n#include <unordered_set> // hash set\n#include <stack>        // LIFO\n#include <queue>        // FIFO\n#include <deque>        // double-ended queue\n\nusing namespace std;\n\nvector<int> v = {3, 1, 4, 1, 5};\nmap<string, int> ages = {{\"Ana\", 25}, {\"Ion\", 30}};\nset<int> unique_nums = {1, 2, 2, 3, 3};  // → {1, 2, 3}\nstack<int> s;\ns.push(1); s.push(2); cout << s.top();  // 2\nqueue<int> q;\nq.push(1); q.push(2); cout << q.front();  // 1\n```\n\n**Complexitate:**\n• vector: O(1) acces, O(1) push_back amortizat\n• map: O(log n) — sorted\n• unordered_map: O(1) average — hash" },
      { order: 2, title: "Algoritmi STL", content: "```cpp\n#include <algorithm>\n#include <vector>\nusing namespace std;\n\nvector<int> v = {3, 1, 4, 1, 5, 9, 2, 6};\n\n// Sortare\nsort(v.begin(), v.end());                    // {1,1,2,3,4,5,6,9}\nsort(v.begin(), v.end(), greater<int>());     // descrescător\n\n// Reverse\nreverse(v.begin(), v.end());\n\n// Find\nauto it = find(v.begin(), v.end(), 4);\nif (it != v.end()) cout << \"găsit la \" << (it - v.begin());\n\n// Min/max\nint min_val = *min_element(v.begin(), v.end());\nint max_val = *max_element(v.begin(), v.end());\n\n// Sum\n#include <numeric>\nint suma = accumulate(v.begin(), v.end(), 0);\n\n// Count\nint c = count(v.begin(), v.end(), 1);\n\n// Filtrare cu copy_if\nvector<int> pare;\ncopy_if(v.begin(), v.end(), back_inserter(pare), [](int x) { return x % 2 == 0; });\n```" },
      { order: 3, title: "Lambda functions", content: "Funcții anonime, perfecte pentru algorithms:\n\n```cpp\nvector<int> v = {5, 2, 8, 1, 9};\n\n// Lambda de bază: [capture](params) -> returnType { body }\nauto patrat = [](int x) { return x * x; };\ncout << patrat(5);  // 25\n\n// Sortare custom\nsort(v.begin(), v.end(), [](int a, int b) {\n  return a > b;  // descrescător\n});\n\n// Cu capture (acces variabile externe)\nint threshold = 5;\nauto count_above = count_if(v.begin(), v.end(), [threshold](int x) {\n  return x > threshold;\n});\n\n// Capture by reference\nint sum = 0;\nfor_each(v.begin(), v.end(), [&sum](int x) {\n  sum += x;\n});\n\n// Capture all by value [=] sau by reference [&]\nauto by_val = [=]() { /* tot e copiat */ };\nauto by_ref = [&]() { /* tot e referință */ };\n```" },
    ],
    tasks: [
      { number: 1, name: "STL container", question: "Pentru access rapid prin index, folosești:", options: ["list", "vector", "map", "set"], answer: "vector", explanation: "vector are O(1) random access. list e linked list — O(n) pentru access prin index.", difficulty: "easy" },
      { number: 2, name: "map vs unordered_map", question: "Diferența map vs unordered_map?", options: ["Identice", "map = sorted (BST, O(log n)); unordered_map = hash (O(1) avg)", "map e legacy", "unordered_map e mai sigur"], answer: "map = sorted (BST, O(log n)); unordered_map = hash (O(1) avg)", explanation: "Pentru iterare ordonată folosești map. Pentru viteză pură: unordered_map (hash).", difficulty: "hard" },
      { number: 3, name: "sort", question: "Cum sortezi un vector?", options: ["v.sort()", "sort(v.begin(), v.end())", "sort(v)", "v->sort()"], answer: "sort(v.begin(), v.end())", explanation: "Algoritmii STL primesc iteratori (range). begin() = primul, end() = după ultimul (exclusive).", difficulty: "easy" },
      { number: 4, name: "Lambda", question: "[](int x) { return x*2; } — ce e?", options: ["Macro", "Lambda — funcție anonimă", "Pointer", "Eroare"], answer: "Lambda — funcție anonimă", explanation: "Lambda C++11+. Sintaxa: [capture](params) { body }. Folosită ca callback în algorithms.", difficulty: "medium" },
      { number: 5, name: "Capture", question: "Ce face [&] în lambda?", options: ["Eroare", "Capture toate variabilele exterioare BY REFERENCE", "Sintaxă invalidă", "Async"], answer: "Capture toate variabilele exterioare BY REFERENCE", explanation: "[&] = ref capture all. [=] = value capture all. [x, &y] = x by value, y by reference. [] = no capture.", difficulty: "hard" },
      { number: 6, name: "set unique", question: "set păstrează duplicate?", options: ["Da", "Nu — păstrează doar valori unice", "Doar primele 100", "Configurabil"], answer: "Nu — păstrează doar valori unice", explanation: "set elimină duplicate automat. multiset permite duplicate. set e și sorted by default.", difficulty: "easy" },
      { number: 7, name: "Iterator", question: "Ce e un iterator?", options: ["Loop counter", "Obiect care marchează poziția într-un container — generalizare a pointer-ului", "For", "Function"], answer: "Obiect care marchează poziția într-un container — generalizare a pointer-ului", explanation: "Iteratori unifică acces la containers: ++it = next, *it = value, it == end() = sfârșit.", difficulty: "hard" },
      { number: 8, name: "stack vs queue", question: "Diferența stack vs queue?", options: ["Identice", "stack = LIFO (last in first out); queue = FIFO (first in first out)", "Doar size", "Niciuna"], answer: "stack = LIFO (last in first out); queue = FIFO (first in first out)", explanation: "Stack: push/pop la același capăt. Queue: push la final, pop la început. Concepts fundamentale.", difficulty: "easy" },
    ],
  },
  {
    slug: "cpp-smart-pointers",
    title: "7. Smart Pointers — gestionare memorie modernă",
    order: 7,
    theory: [
      { order: 1, title: "Problema: memory leaks cu raw pointers", content: "```cpp\n// Raw pointer — periculos\nvoid bad() {\n  int* p = new int(42);\n  // ... cod care poate arunca exception\n  if (something_wrong()) {\n    return;  // ❌ memory leak — delete nu se mai apelează!\n  }\n  delete p;\n}\n\n// Cu smart pointer — auto-cleanup\n#include <memory>\n\nvoid good() {\n  auto p = std::make_unique<int>(42);\n  // p se eliberează automat la sfârșitul scope-ului\n  // chiar și la return early sau exception\n}\n```\n\n**RAII** (Resource Acquisition Is Initialization) = resursele sunt eliberate automat când obiectele ies din scope. Smart pointers implementează RAII pentru memoria heap." },
      { order: 2, title: "unique_ptr — un singur owner", content: "```cpp\n#include <memory>\nusing namespace std;\n\n// Creare\nauto p = make_unique<int>(42);\nauto v = make_unique<vector<int>>(10, 0);\n\ncout << *p;  // 42\np->...;        // pentru class methods\n\n// NU se poate copia (un singur owner)\n// auto q = p;  // ❌ eroare\n\n// Se poate MUTA (transfer ownership)\nauto q = std::move(p);\n// p e acum nullptr, q deține valoarea\n\n// Eliberare manuală\np.reset();   // eliberează și setează nullptr\np = nullptr; // echivalent\n\n// Acces la raw pointer\nint* raw = p.get();  // fără transfer ownership\n\n// Custom deleter\nauto file = unique_ptr<FILE, decltype(&fclose)>(fopen(\"x.txt\", \"r\"), fclose);\n```\n\n**Use case:** majoritatea cazurilor în care ai un singur owner clar." },
      { order: 3, title: "shared_ptr și weak_ptr", content: "**shared_ptr** — multiple owners (reference counted):\n\n```cpp\nauto p1 = make_shared<int>(42);\nauto p2 = p1;  // OK — ambii dețin\ncout << p1.use_count();  // 2\n\n{\n  auto p3 = p1;\n  cout << p1.use_count();  // 3\n}  // p3 ies din scope — count 2\n\n// Ultimul shared_ptr eliberează memoria\n```\n\n**weak_ptr** — observă fără a deține (rupe cicluri):\n\n```cpp\n// Problema: ciclu de referințe\nstruct Node {\n  shared_ptr<Node> next;  // ❌ poate cauza leak ciclic\n  weak_ptr<Node> parent;  // ✅ nu incrementează count\n};\n\nweak_ptr<int> wp = p1;\nif (auto sp = wp.lock()) {  // promovează la shared_ptr dacă încă există\n  cout << *sp;\n}\n```\n\n**Modern C++ rule:** **NU folosi `new`/`delete` direct.** Folosește `make_unique` și `make_shared`." },
    ],
    tasks: [
      { number: 1, name: "RAII", question: "Ce e RAII?", options: ["Random Access...", "Resource Acquisition Is Initialization — resursele sunt legate de viața obiectelor", "Bibliotecă", "Compilare"], answer: "Resource Acquisition Is Initialization — resursele sunt legate de viața obiectelor", explanation: "Pattern fundamental C++: alocă în constructor, eliberează în destructor. Cleanup automat la ieșire scope.", difficulty: "hard" },
      { number: 2, name: "unique_ptr", question: "Ce face unique_ptr?", options: ["Pointer simplu", "Smart pointer cu un singur owner — eliberează automat memoria", "Reference", "Macro"], answer: "Smart pointer cu un singur owner — eliberează automat memoria", explanation: "unique_ptr deține exclusiv. La ieșire scope sau reset, memoria e eliberată automat.", difficulty: "medium" },
      { number: 3, name: "make_unique", question: "Cum creezi un unique_ptr<int> cu valoarea 5?", options: ["new unique_ptr<int>(5)", "make_unique<int>(5)", "unique_ptr<int>(5)", "Toate"], answer: "make_unique<int>(5)", explanation: "make_unique e factory function recomandată — exception-safe și eficient. Evitezi new direct.", difficulty: "medium" },
      { number: 4, name: "shared_ptr count", question: "Cum afli câți owners are un shared_ptr?", options: ["p.size()", "p.use_count()", "p.refs()", "p.length()"], answer: "p.use_count()", explanation: "use_count() returnează numărul de shared_ptr care dețin obiectul. Util pentru debugging.", difficulty: "medium" },
      { number: 5, name: "shared_ptr cycle", question: "Ce problemă poate apărea cu shared_ptr ciclic?", options: ["Niciuna", "Memory leak — dacă A.next = B și B.prev = A (shared), count nu ajunge la 0", "Crash", "Performanță"], answer: "Memory leak — dacă A.next = B și B.prev = A (shared), count nu ajunge la 0", explanation: "Reference cycle: A → B → A. Niciun count nu ajunge la 0. Soluție: weak_ptr pentru link-uri 'back'.", difficulty: "hard" },
      { number: 6, name: "weak_ptr", question: "Ce face weak_ptr?", options: ["Pointer slab", "Observă un shared_ptr fără a incrementa count — rupe cicluri", "Defect", "Nullable shared_ptr"], answer: "Observă un shared_ptr fără a incrementa count — rupe cicluri", explanation: "weak_ptr nu deține. .lock() returnează shared_ptr dacă obiectul încă există, sau nullptr.", difficulty: "hard" },
      { number: 7, name: "move", question: "auto q = std::move(p) cu unique_ptr — ce se întâmplă?", options: ["Copiere", "Transfer ownership: p devine nullptr, q deține valoarea", "Crash", "Eroare"], answer: "Transfer ownership: p devine nullptr, q deține valoarea", explanation: "Move semantics — transferă proprietatea fără a copia. Esențial pentru unique_ptr (nu poate fi copiat).", difficulty: "hard" },
      { number: 8, name: "Modern rule", question: "În C++ modern, când folosești `new`?", options: ["Mereu", "Aproape niciodată — preferă make_unique/make_shared", "Pentru orice obiect", "Doar mici obiecte"], answer: "Aproape niciodată — preferă make_unique/make_shared", explanation: "Modern C++: smart pointers > raw pointers. Evită new/delete direct. RAII previne leaks.", difficulty: "medium" },
    ],
  },
  {
    slug: "cpp-exceptions",
    title: "8. Exception Handling",
    order: 8,
    theory: [
      { order: 1, title: "try / catch / throw", content: "```cpp\n#include <iostream>\n#include <stdexcept>\n#include <string>\nusing namespace std;\n\ndouble impart(int a, int b) {\n  if (b == 0) {\n    throw runtime_error(\"Împărțire la zero!\");\n  }\n  return (double)a / b;\n}\n\nint main() {\n  try {\n    cout << impart(10, 2) << endl;  // 5\n    cout << impart(10, 0) << endl;  // throw!\n    cout << \"Nu se afișează\\n\";\n  } catch (const runtime_error& e) {\n    cout << \"Eroare: \" << e.what() << endl;\n  } catch (const exception& e) {\n    cout << \"Generic: \" << e.what() << endl;\n  } catch (...) {\n    cout << \"Orice altceva\\n\";\n  }\n  cout << \"Continuă...\\n\";\n  return 0;\n}\n```\n\n**Tipuri standard:** `runtime_error`, `logic_error`, `out_of_range`, `invalid_argument`, `bad_alloc`." },
      { order: 2, title: "Stack unwinding", content: "Când e aruncată o excepție, **stack-ul e desfășurat** — destructorii obiectelor locale sunt apelați:\n\n```cpp\nclass Resource {\n public:\n  Resource() { cout << \"Acquire\\n\"; }\n  ~Resource() { cout << \"Release\\n\"; }\n};\n\nvoid funcCarePoateAruncaException() {\n  Resource r;  // alocat\n  throw runtime_error(\"oops\");\n  // ~Resource() apelat AUTOMAT (stack unwinding)\n}\n\nint main() {\n  try {\n    funcCarePoateAruncaException();\n  } catch (...) {\n    cout << \"Gestionat\\n\";\n  }\n}\n// Output:\n// Acquire\n// Release   ← destructor apelat la unwinding\n// Gestionat\n```\n\nAcesta e **motivul pentru care RAII e crucial** — resurse eliberate automat chiar și la exception." },
      { order: 3, title: "Exception custom + best practices", content: "```cpp\nclass ValidationError : public exception {\n  string mesaj;\n public:\n  ValidationError(const string& m) : mesaj(m) {}\n  const char* what() const noexcept override {\n    return mesaj.c_str();\n  }\n};\n\nvoid validateAge(int age) {\n  if (age < 0 || age > 150) {\n    throw ValidationError(\"Vârstă invalidă: \" + to_string(age));\n  }\n}\n\ntry {\n  validateAge(-5);\n} catch (const ValidationError& e) {\n  cerr << \"Validare: \" << e.what() << endl;\n}\n```\n\n**Best practices:**\n• Aruncă **prin valoare**, prinde **prin const reference**\n• Folosește exception din `<stdexcept>` sau derivate\n• `noexcept` pentru funcții care nu aruncă\n• Nu arunca în destructor (terminează programul)" },
    ],
    tasks: [
      { number: 1, name: "throw", question: "Cum arunci o excepție în C++?", options: ["raise Error()", "throw runtime_error(\"msg\")", "error()", "exit(1)"], answer: "throw runtime_error(\"msg\")", explanation: "throw urmat de o expresie. Tipic obiect din ierarhia <exception>.", difficulty: "easy" },
      { number: 2, name: "catch all", question: "Ce face catch (...)?", options: ["Sintaxă invalidă", "Prinde ORICE excepție (de ultimă instanță)", "Doar exception", "Eroare"], answer: "Prinde ORICE excepție (de ultimă instanță)", explanation: "catch (...) prinde tot. Util pentru cleanup / re-throw, dar nu obține informații despre excepție.", difficulty: "medium" },
      { number: 3, name: "stack unwinding", question: "Ce se întâmplă cu obiectele locale la o excepție?", options: ["Memory leak", "Destructorii sunt apelați automat (stack unwinding)", "Crash", "Frozen"], answer: "Destructorii sunt apelați automat (stack unwinding)", explanation: "Stack unwinding garantează cleanup-ul resurselor. Combinat cu RAII = exception-safe code.", difficulty: "hard" },
      { number: 4, name: "Catch by reference", question: "Best practice: prinde excepții prin:", options: ["Valoare", "const reference (const exception&)", "Pointer", "Niciuna"], answer: "const reference (const exception&)", explanation: "Prin valoare = slicing (pierzi info derived). Prin pointer = manual cleanup. const& = corect și eficient.", difficulty: "medium" },
      { number: 5, name: "what()", question: "Ce face e.what()?", options: ["Crash", "Returnează mesajul erorii ca const char*", "Resetează", "Re-aruncă"], answer: "Returnează mesajul erorii ca const char*", explanation: ".what() e metoda virtuală a std::exception. Override-uită pentru a returna mesaj specific.", difficulty: "medium" },
      { number: 6, name: "Custom exception", question: "Cum creezi un tip de exception custom?", options: ["throw class", "Moștenește din std::exception (sau derivat)", "Doar string-uri", "Imposibil"], answer: "Moștenește din std::exception (sau derivat)", explanation: "class MyError : public exception { ... }. Override what() pentru mesaj custom.", difficulty: "medium" },
      { number: 7, name: "noexcept", question: "Ce face cuvântul cheie noexcept pe o funcție?", options: ["Nu execută", "Promite că funcția nu aruncă excepții (optimizări compilator)", "Macro", "Sincron"], answer: "Promite că funcția nu aruncă excepții (optimizări compilator)", explanation: "noexcept permite optimizări. Dacă totuși aruncă, programul terminate. Util pentru destructori, move ops.", difficulty: "hard" },
      { number: 8, name: "throw în destructor", question: "Ar trebui să arunci excepții din destructor?", options: ["Da, oricum", "NU — poate cauza terminate dacă deja unwinding", "Doar pentru bug-uri", "Mereu"], answer: "NU — poate cauza terminate dacă deja unwinding", explanation: "Dacă destructor aruncă în timpul unwinding, programul e terminat (std::terminate). Tratează errors în destructor.", difficulty: "hard" },
    ],
  },
  {
    slug: "cpp-references-modern",
    title: "9. References, move semantics, modern features",
    order: 9,
    theory: [
      { order: 1, title: "References (&) — alias-uri", content: "```cpp\nint x = 10;\nint& ref = x;     // ref e alias pentru x\nref = 20;          // modifică x! x e acum 20\ncout << x;         // 20\n\n// Reference TREBUIE inițializată\n// int& y;        // ❌ eroare\n\n// Reference NU poate fi reasignată\nint a = 1, b = 2;\nint& r = a;\nr = b;            // a = 2 (asignare, nu rebind!)\n\n// const reference\nconst int& cr = x;\n// cr = 5;        // ❌ eroare\n\n// Pass by reference (eficient pentru obiecte mari)\nvoid modifica(int& val) { val = 100; }\nvoid afisare(const string& s) { cout << s; }  // const = no copy\n\nint n = 5;\nmodifica(n);  // n devine 100\n```\n\n**References vs Pointers:**\n• Reference NU poate fi NULL\n• Reference NU poate fi reasignată\n• Reference se folosește ca variabila originală (`.` nu `->`)" },
      { order: 2, title: "Move semantics (rvalue references)", content: "**lvalue** = are nume (variabilă). **rvalue** = temporar (literal, return value).\n\n```cpp\nstring s1 = \"Hello\";\nstring s2 = s1;            // copy — s1 încă conține \"Hello\"\nstring s3 = std::move(s1);  // MOVE — s1 e acum gol, s3 conține \"Hello\"\n\n// move e eficient — fără copiere de date interne\n// Util pentru transfer de resurse (vector, string, smart pointers)\n\n// Move constructor\nclass Buffer {\n  int* data;\n public:\n  Buffer(Buffer&& other) noexcept   // && = rvalue reference\n    : data(other.data) {\n    other.data = nullptr;\n  }\n  // Move assignment\n  Buffer& operator=(Buffer&& other) noexcept {\n    if (this != &other) {\n      delete[] data;\n      data = other.data;\n      other.data = nullptr;\n    }\n    return *this;\n  }\n};\n```\n\n**Avantaj:** transfer de ownership rapid (no deep copy)." },
      { order: 3, title: "Auto, structured bindings, ranges (C++17/20)", content: "```cpp\n// auto — type deduction\nauto x = 42;            // int\nauto pi = 3.14;          // double\nauto& ref = x;            // int& (cu &)\nauto* p = &x;             // int*\n\nfor (const auto& [key, value] : myMap) {  // C++17 structured bindings\n  cout << key << \": \" << value << endl;\n}\n\n// std::pair destructuring\nauto [a, b] = std::pair{1, 2};\n\n// std::optional (C++17) — valoare opțională\n#include <optional>\nstd::optional<int> findIndex(vector<int>& v, int x) {\n  for (size_t i = 0; i < v.size(); i++) {\n    if (v[i] == x) return i;\n  }\n  return std::nullopt;\n}\n\nauto idx = findIndex(v, 5);\nif (idx) cout << \"Găsit la \" << *idx;\n\n// std::variant (C++17) — type-safe union\nstd::variant<int, string> v = 42;\nv = \"hello\";\n```" },
    ],
    tasks: [
      { number: 1, name: "Reference", question: "Ce e int& ref = x?", options: ["Pointer", "Alias pentru x — modificarea ref afectează x", "Copy", "Const"], answer: "Alias pentru x — modificarea ref afectează x", explanation: "Reference e nume alternativ pentru aceeași variabilă. Folosit pentru pass by reference în funcții.", difficulty: "easy" },
      { number: 2, name: "Reference vs pointer", question: "Care e diferența cheie reference vs pointer?", options: ["Identice", "Reference nu poate fi NULL/reasignată; pointer da", "Pointer e mai rapid", "Reference doar pentru int"], answer: "Reference nu poate fi NULL/reasignată; pointer da", explanation: "Reference = alias permanent. Pointer = adresă variabilă. Reference e safer (no NULL), pointer e flexibil.", difficulty: "medium" },
      { number: 3, name: "const&", question: "void f(const string& s) — de ce const&?", options: ["Stilizare", "Eviți copierea (eficient) și garantezi că nu modifici", "Mai vechi", "Niciun motiv"], answer: "Eviți copierea (eficient) și garantezi că nu modifici", explanation: "Pass by const reference = no copy + safety. Standard pentru parametri non-primitive read-only.", difficulty: "medium" },
      { number: 4, name: "std::move", question: "Ce face std::move?", options: ["Copiere", "Cast la rvalue reference — permite move în loc de copy", "Mută în memorie", "Crash"], answer: "Cast la rvalue reference — permite move în loc de copy", explanation: "std::move e doar un cast. Compilatorul alege move constructor/operator dacă există. Original devine 'goalit'.", difficulty: "hard" },
      { number: 5, name: "auto", question: "auto x = 42; — ce tip are x?", options: ["auto", "int (dedus)", "Variant", "Eroare"], answer: "int (dedus)", explanation: "auto deduce tipul din expresie. 42 e int literal → x e int.", difficulty: "easy" },
      { number: 6, name: "Structured binding", question: "Ce face auto [a, b] = pair{1, 2};?", options: ["Eroare", "Destructuring — a=1, b=2", "Array", "Toate"], answer: "Destructuring — a=1, b=2", explanation: "Structured bindings (C++17) extrag din pair, tuple, struct etc. Curat și clar.", difficulty: "medium" },
      { number: 7, name: "optional", question: "Pentru ce e std::optional<T>?", options: ["Necesar", "Reprezintă o valoare care POATE lipsi (alternativă la pointers/sentinel)", "Performance", "Niciuna"], answer: "Reprezintă o valoare care POATE lipsi (alternativă la pointers/sentinel)", explanation: "optional<int> = poate avea int sau nullopt. Semantic clar 'this might be missing'. Type-safe.", difficulty: "hard" },
      { number: 8, name: "Move efficient", question: "De ce e move mai eficient decât copy pentru string mare?", options: ["Mai puțin cod", "Transfer pointer intern în loc de a copia bytes", "Mai pleasant la ochi", "Niciuna"], answer: "Transfer pointer intern în loc de a copia bytes", explanation: "string conține pointer la buffer. Move = doar swap pointers. Copy = aloca + memcpy bytes.", difficulty: "hard" },
    ],
  },
  {
    slug: "cpp-proiect-magazin",
    title: "10. Proiect: Sistem de inventar magazin",
    order: 10,
    theory: [
      { order: 1, title: "Structura proiectului", content: "Construim un mini-sistem pentru a gestiona produse într-un magazin:\n\n**Funcționalități:**\n• Adaugă produse\n• Listează toate\n• Caută după nume\n• Filtrează după preț\n• Actualizează stoc\n• Salvează în fișier (persistență)\n\n```cpp\n// product.h\nstruct Product {\n  int id;\n  std::string nume;\n  double pret;\n  int stoc;\n};\n\n// inventory.h\nclass Inventory {\n  std::vector<Product> items;\n  int nextId = 1;\n public:\n  void add(const std::string& nume, double pret, int stoc);\n  void list() const;\n  Product* find(int id);\n  std::vector<Product*> searchByName(const std::string& q);\n  void updateStock(int id, int delta);\n  void saveToFile(const std::string& fname);\n  void loadFromFile(const std::string& fname);\n};\n```" },
      { order: 2, title: "Implementare metode esențiale", content: "```cpp\n#include \"inventory.h\"\n#include <iostream>\n#include <fstream>\n#include <algorithm>\nusing namespace std;\n\nvoid Inventory::add(const string& nume, double pret, int stoc) {\n  items.push_back({nextId++, nume, pret, stoc});\n}\n\nvoid Inventory::list() const {\n  cout << \"\\n=== INVENTAR ===\\n\";\n  for (const auto& p : items) {\n    cout << \"#\" << p.id << \" | \" << p.nume\n         << \" | \" << p.pret << \" RON\"\n         << \" | stoc: \" << p.stoc << endl;\n  }\n}\n\nProduct* Inventory::find(int id) {\n  auto it = std::find_if(items.begin(), items.end(),\n    [id](const Product& p) { return p.id == id; });\n  return it != items.end() ? &(*it) : nullptr;\n}\n\nvector<Product*> Inventory::searchByName(const string& q) {\n  vector<Product*> result;\n  for (auto& p : items) {\n    if (p.nume.find(q) != string::npos) {\n      result.push_back(&p);\n    }\n  }\n  return result;\n}\n\nvoid Inventory::updateStock(int id, int delta) {\n  if (auto* p = find(id)) {\n    p->stoc += delta;\n  }\n}\n```" },
      { order: 3, title: "Persistență fișier + main", content: "```cpp\nvoid Inventory::saveToFile(const string& fname) {\n  ofstream f(fname);\n  f << items.size() << \"\\n\";\n  for (const auto& p : items) {\n    f << p.id << \"|\" << p.nume << \"|\" << p.pret << \"|\" << p.stoc << \"\\n\";\n  }\n}\n\nvoid Inventory::loadFromFile(const string& fname) {\n  ifstream f(fname);\n  if (!f) return;\n  size_t n;\n  f >> n;\n  f.ignore();  // consume newline\n  for (size_t i = 0; i < n; i++) {\n    Product p;\n    char sep;\n    f >> p.id >> sep;\n    getline(f, p.nume, '|');\n    f >> p.pret >> sep >> p.stoc;\n    f.ignore();\n    items.push_back(p);\n    if (p.id >= nextId) nextId = p.id + 1;\n  }\n}\n\n// main.cpp\nint main() {\n  Inventory inv;\n  inv.loadFromFile(\"inv.txt\");\n\n  while (true) {\n    cout << \"\\n1) Add  2) List  3) Search  4) Update stock  5) Save & Quit\\n> \";\n    int op; cin >> op;\n    if (op == 1) {\n      string nume; double pret; int stoc;\n      cin.ignore();\n      cout << \"Nume: \"; getline(cin, nume);\n      cout << \"Preț: \"; cin >> pret;\n      cout << \"Stoc: \"; cin >> stoc;\n      inv.add(nume, pret, stoc);\n    } else if (op == 2) inv.list();\n    else if (op == 3) {\n      string q; cin.ignore(); getline(cin, q);\n      for (auto* p : inv.searchByName(q)) cout << p->nume << endl;\n    } else if (op == 5) {\n      inv.saveToFile(\"inv.txt\"); break;\n    }\n  }\n}\n```" },
    ],
    tasks: [
      { number: 1, name: "Project structure", question: "Pentru un sistem de inventar, e bine să separi:", options: ["Tot într-un fișier", "Header pentru declarații, .cpp pentru implementare", "Doar funcții libere", "Niciun"], answer: "Header pentru declarații, .cpp pentru implementare", explanation: "Convenție C++: .h pentru API public, .cpp pentru implementare. Permite compilare separată.", difficulty: "easy" },
      { number: 2, name: "find_if", question: "Pentru a găsi un produs după ID:", options: ["for loop manual", "std::find_if cu lambda — concis și expresiv", "Imposibil", "Doar map"], answer: "std::find_if cu lambda — concis și expresiv", explanation: "find_if + lambda e idiomatic C++ modern. Returnează iterator la primul match (sau end).", difficulty: "medium" },
      { number: 3, name: "string::npos", question: "Ce returnează str.find(\"x\") dacă nu găsește?", options: ["-1", "string::npos (un size_t mare)", "0", "NULL"], answer: "string::npos (un size_t mare)", explanation: "string::npos = static const size_t maxim. Compari cu npos pentru a verifica 'not found'.", difficulty: "medium" },
      { number: 4, name: "ofstream", question: "Pentru a scrie într-un fișier:", options: ["printf", "ofstream f(\"file\"); f << ...;", "FILE*", "fopen"], answer: "ofstream f(\"file\"); f << ...;", explanation: "C++ stream-uri: ofstream pentru output, ifstream pentru input. Operator << identic cu cout.", difficulty: "medium" },
      { number: 5, name: "References stocking", question: "vector<Product*> vs vector<Product> — care păstrează pointers la elementele inventarului?", options: ["vector<Product>", "vector<Product*> — pointers la elementele din inventar", "Identice", "Niciuna"], answer: "vector<Product*> — pointers la elementele din inventar", explanation: "Pentru a returna referințe la elemente fără copy, folosești vector<T*>. Atenție: pointers invalidați la realloc.", difficulty: "hard" },
      { number: 6, name: "Persistence", question: "Cum salvezi date între execuții?", options: ["Memory", "Fișiere (text sau binary)", "Imposibil", "Doar DB"], answer: "Fișiere (text sau binary)", explanation: "ofstream/ifstream pentru fișiere. Format ales: text (CSV, JSON) sau binary (mai compact).", difficulty: "easy" },
      { number: 7, name: "Auto-increment", question: "Cum gestionezi ID-uri unice?", options: ["Random", "Counter intern (nextId++) — simplu și eficient", "Hash time", "Niciun"], answer: "Counter intern (nextId++) — simplu și eficient", explanation: "Counter pe clasa Inventory: thread-unsafe dar OK pentru single-threaded. Pentru DB real folosești auto-increment.", difficulty: "medium" },
      { number: 8, name: "Const-correctness", question: "void list() const — ce înseamnă const la final?", options: ["Imposibil", "Promisiune că metoda nu modifică starea — apelabilă pe obiecte const", "Static", "Pure virtual"], answer: "Promisiune că metoda nu modifică starea — apelabilă pe obiecte const", explanation: "const member function — best practice pentru getter/display. Compilatorul previne modificări accidentale.", difficulty: "medium" },
    ],
  },
];

module.exports = { cppLessons };
